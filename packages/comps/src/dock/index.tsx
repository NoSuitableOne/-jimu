import React from 'react';

type Props = {
}; 

const Dock: React.FC<Props> = props => {
  const {  } = props;

  return (
    <div className={'glass'}>
      <ul className={'dock'}>
        <li>😄</li>
      </ul>
    </div>
  );
};

export default Dock; 
