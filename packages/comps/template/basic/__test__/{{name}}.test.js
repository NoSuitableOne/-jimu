import React from "react";
import { render, screen, fireEvent } from "@testing-library/react";
import {{pascalCase name}} from '../index';

describe('@budlocks/comps/<{{pascalCase name}} />', () => {
  it("test <{{pascalCase name}} />", () => {
    render(<{{pascalCase name}} />);

  });
});